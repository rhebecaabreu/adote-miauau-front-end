import React from "react";
import styled from "styled-components";
import Button from "components/atoms/Button";

const Root = styled.div`
  h6 {
    color: #fff;
    margin-top: 0;
    text-transform: uppercase;
  }
`;

const PetInfoHealth = () => (
  <Root>
    <p>PetInfoHealth</p>
  </Root>
);

export default PetInfoHealth;
